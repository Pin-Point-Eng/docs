---
title: "Tenant Portal"
description: "Let anyone on the property submit a request — no account needed."
---

The Tenant Portal is a dedicated web link that lets tenants, contractors, or anyone else on the property submit a request directly into PIN — without needing an account. Every submission becomes a Pin in the system, routed to the right department, and tracked through to completion alongside everything else.

## How It Works

Anyone can submit a request by visiting your venue's PIN portal link or scanning a QR code. The portal link is on the **home screen of the PIN web app**, below the create button. QR codes are generated from the **Pin QR Generator** (see below).

No account or login is required. The person submitting fills out a short form — title, description, optional photos, and a location if the QR code does not already include one — and the request is created as a Pin and routed to the team set on the QR code or portal link. 

<Tip>
  QR codes work well posted in tenant spaces and back hallways. The portal link can be shared via email, signage, the tenant handbook, or any other channel. Both the link and QR codes can be reused indefinitely — no need to refresh or rotate them.
</Tip>

<Frame>
  <img
    src="/images/IMG_1421-landscape.png"
    alt="How It Works screen in PIN"
    title="How It Works screen in PIN"
    style={{ width: "100%", display: "block" }}
  />
</Frame>

## Pin QR Generator

Use the **Pin QR Generator** to create signed QR codes that route submissions to a specific team — for example, the front desk, housekeeping, or maintenance — with an optional pre-set location.

Open it in two places:

- **Create menu** in the sidebar → **Pin QR Generator** for any team and (optionally) any location.
- A location's page in the **Locations** module → **QR Code** button, which pre-fills that location.

To generate a QR code:

1. Select the **Team** that should receive submissions from this QR code.
2. Optionally select a **Location**. If set, the location is locked into the submission and the submitter cannot change it.
3. Select **Generate**.
4. **Copy the link** to share digitally, or **Download PNG** to print a poster that includes the location name, team, and venue.

<Tip>
  Generate a separate QR code for each printed location — for example, one per guest room, conference room, or restroom — and select the team that owns that space. The submitter never has to choose, so requests always land with the right team.
</Tip>

Each QR code is cryptographically signed to the team and optional location chosen when it is generated, so different codes across the property route to different teams automatically — there is no venue-wide default. If the team or location encoded in a code is later changed or removed, the code stops working and the submitter is asked to scan an updated one; regenerate it from the Pin QR Generator to replace it.

## Routing

Routing is set **per QR code**, not at the organization level. When someone generates a QR code, they pick the team that should receive submissions from that specific code:

- A code posted in a tenant suite can route to the front desk.
- A code on a piece of equipment can route to maintenance.
- A code in a bathroom can route to housekeeping.

The portal link on the web app home screen routes to the team configured for the venue's default portal submission flow.

## QR Code Options

PIN supports both property-wide and location-specific QR codes:

- **Property-wide QR code** — available on the web app home screen. The submitter selects their location before sending the request.
- **Location-specific QR codes** — available from each location page. The location is pre-set automatically when scanned.

Location-specific QR codes are best for tenant spaces because they remove location selection from the submission process and help every request land in the right place.

Every request is logged the moment it is submitted — nothing gets lost.

<Frame>
  <img
    src="/images/IMG_1407-landscape.png"
    alt="Routing Options screen in PIN"
    title="Routing Options screen in PIN"
    style={{ width: "100%", display: "block" }}
  />
</Frame>

## Email Updates to the Submitter

When filling out the form, the submitter can opt in to email updates. If they opt in, they automatically receive emails as the Pin's status changes — keeping them informed from intake through completion without anyone on the team having to chase them down.

Email updates are the submitter's choice at the time of submission.

## How Tenant Pins Behave

Once submitted, tenant Pins follow the same workflow, status updates, and completion flow as any other Pin in the system. They are tagged so the team can identify them at a glance.

## What Tenants Typically Submit

The portal is primarily used by tenants for maintenance and service requests — broken fixtures, HVAC issues, signage problems, and other support needs. It removes the need for phone calls, texts, or emails to report issues.

<Card title="Quick Reference Guide" icon="arrow-right" href="/quick-reference/quick-reference-guide">
  A full list of common tasks and how to complete them.
</Card>